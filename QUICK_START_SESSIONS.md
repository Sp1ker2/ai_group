# 🚀 Быстрый старт: Загрузка сессий и запуск

## Что нужно сделать

### ✅ Шаг 1: Получить Telegram API credentials

1. Идите на https://my.telegram.org/apps
2. Войдите
3. Создайте приложение
4. Скопируйте `api_id` и `api_hash`

### ✅ Шаг 2: Получить session для аккаунтов

```bash
# Установить переменные
export TELEGRAM_API_ID="ваш_api_id"
export TELEGRAM_API_HASH="ваш_api_hash"

# Получить session (для каждого аккаунта)
python scripts/get-session.py
```

Скрипт создаст файл `session_<account_id>.json` для каждого аккаунта.

### ✅ Шаг 3: Загрузить сессии в MinIO

```bash
# Один файл
python scripts/upload-session-to-s3.py session_12345.json

# Или все сразу
python scripts/batch-upload-sessions.py
```

### ✅ Шаг 4: Запустить Worker

После загрузки session, Worker **автоматически**:
- ✅ Загрузит session из S3
- ✅ Авторизуется БЕЗ кода
- ✅ Начнет выполнять задачи

## 🎯 Как это работает

```
1. Session в S3 → 2. Worker загружает → 3. Авторизация → 4. Готово!
```

**Никаких кодов не требуется!** Session содержит всю информацию для авторизации.

## 📝 Пример полного процесса

```bash
# 1. Получить session для аккаунта +79001234567
export TELEGRAM_API_ID="12345"
export TELEGRAM_API_HASH="abcdef..."
python scripts/get-session.py
# Введите номер: +79001234567
# Введите код из Telegram: 12345
# ✅ Создан файл: session_12345.json

# 2. Загрузить в MinIO
python scripts/upload-session-to-s3.py session_12345.json
# ✅ Session загружен в S3

# 3. Запустить Worker (в Kubernetes или Docker)
# Worker автоматически использует session и авторизуется!
```

## ❓ FAQ

**Q: Нужно ли каждый раз вводить код?**  
A: Нет! После получения session, авторизация автоматическая.

**Q: Могут ли аккаунты общаться?**  
A: Да! После авторизации Worker может отправлять/получать сообщения.

**Q: Сколько раз нужно получать session?**  
A: Один раз на аккаунт. Session действует до смены пароля или logout.

## ✅ Готово!

После выполнения этих шагов:
- ✅ Сессии загружены в S3
- ✅ Worker автоматически авторизуется
- ✅ Аккаунты готовы к работе
- ✅ Можно запускать warm-up задачи
