# 💬 Групповое общение между аккаунтами

## 🎯 Возможности

Система поддерживает автоматическое создание групп и общение между аккаунтами:

- ✅ Создание групп
- ✅ Добавление участников
- ✅ Отправка сообщений
- ✅ Чтение сообщений
- ✅ Симуляция общения

## 📋 Настройка

### 1. Включить групповое общение в Worker

В Job манифесте или переменных окружения:

```yaml
env:
- name: ENABLE_GROUP_CHAT
  value: "true"
- name: GROUP_ID
  value: "123456789"  # ID существующей группы
- name: GROUP_TITLE
  value: "Warm-up Chat"  # Название группы (для создания)
- name: MEMBER_PHONES
  value: "+79001234568,+79001234569"  # Номера участников
```

### 2. Создать группу вручную

```bash
# Создать группу и добавить участников
python scripts/create-group-chat.py create \
  "Warm-up Chat" \
  +79001234567 \
  +79001234568 \
  +79001234569
```

### 3. Автоматическое создание группы

```bash
# Создать группу со всеми аккаунтами из local-storage
python scripts/group-chat-automation.py create \
  "Warm-up Chat" \
  +79001234567
```

## 🔄 Как это работает

### Сценарий 1: Создание группы

```
1. Админ аккаунт создает группу
2. Добавляются все участники
3. Сохраняется GROUP_ID
4. Все Worker'ы используют этот GROUP_ID
```

### Сценарий 2: Общение в группе

```
Worker 1 → Отправляет сообщение в группу
Worker 2 → Читает сообщения
Worker 3 → Отправляет ответ
... (цикл продолжается)
```

### Сценарий 3: Симуляция общения

```bash
# Автоматическая симуляция общения
python scripts/group-chat-automation.py simulate <group_id> 10
```

## 📝 Примеры использования

### Пример 1: Создание группы

```python
# Через скрипт
python scripts/create-group-chat.py create \
  "Test Group" \
  +79001234567 \
  +79001234568 \
  +79001234569

# Результат:
# ✅ Группа создана: 123456789
# ✅ Добавлено участников: 2/2
```

### Пример 2: Отправка сообщений

```python
# Отправить сообщение от аккаунта
python scripts/create-group-chat.py send \
  123456789 \
  +79001234567 \
  "Привет всем!"

# Результат:
# ✅ Сообщение отправлено от +79001234567
```

### Пример 3: Автоматическое общение

```python
# Отправить сообщения по кругу
python scripts/group-chat-automation.py send \
  123456789 \
  "Привет!" \
  "Как дела?" \
  "Все отлично!"

# Каждое сообщение отправляется от следующего аккаунта
```

## 🎯 Интеграция в Worker

Worker автоматически выполняет групповые действия если включено:

```python
# В worker.py автоматически:
if ENABLE_GROUP_CHAT:
    - Создает/присоединяется к группе
    - Отправляет сообщения
    - Читает сообщения
    - Взаимодействует с другими аккаунтами
```

## 📊 Пример полного цикла

### 1. Подготовка

```bash
# Все сессии уже получены
local-storage/sessions/
├── 79001234567.json
├── 79001234568.json
└── 79001234569.json
```

### 2. Создание группы

```bash
python scripts/create-group-chat.py create \
  "Warm-up Chat" \
  +79001234567 \
  +79001234568 \
  +79001234569
```

### 3. Настройка Worker'ов

```yaml
# В Job манифесте
env:
- name: ENABLE_GROUP_CHAT
  value: "true"
- name: GROUP_ID
  value: "123456789"
```

### 4. Запуск Worker'ов

```bash
# Каждый Worker:
# 1. Авторизуется
# 2. Присоединяется к группе
# 3. Отправляет/читает сообщения
# 4. Отчитывается
```

## 🔧 Дополнительные функции

### Чтение сообщений

```python
# Worker автоматически читает последние сообщения
messages = await worker._read_group_messages(group_id, limit=10)
```

### Добавление участников

```python
# Добавить новых участников в группу
await worker._add_members_to_group(
    group_id=123456789,
    phone_numbers=['+79001234570', '+79001234571']
)
```

## ⚙️ Настройки

### Переменные окружения

- `ENABLE_GROUP_CHAT=true` - включить групповое общение
- `GROUP_ID=123456789` - ID группы
- `GROUP_TITLE=Warm-up Chat` - название группы (для создания)
- `MEMBER_PHONES=+79001234568,+79001234569` - номера участников
- `MESSAGE_TEXT=Hello!` - текст сообщения

## ✅ Итог

Система поддерживает:
- ✅ Создание групп
- ✅ Добавление участников
- ✅ Общение между аккаунтами
- ✅ Автоматическую симуляцию общения
- ✅ Интеграцию в Worker для автоматического выполнения

Все аккаунты могут общаться между собой в группах!




