# 🎨 Руководство по веб-интерфейсу

## 🚀 Быстрый старт

### Запуск

```bash
docker-compose up -d control-api
```

Откройте в браузере: **http://localhost:8000**

## 📱 Страницы интерфейса

### 1. Главная страница (`/`)

- Обзор системы
- Быстрый доступ к основным функциям
- Статистика

### 2. Dashboard (`/dashboard`)

**Возможности:**
- Общая статистика (сессии, группы, задачи)
- Статус системы
- Последние сессии
- Последние группы

**Автообновление:** каждые 30 секунд

### 3. Sessions (`/sessions`)

**Возможности:**
- Просмотр всех сессий
- Информация о каждой сессии:
  - Номер телефона
  - Имя файла
  - Статус
  - Дата создания
- Тестирование сессий

**Действия:**
- Обновить список (кнопка Refresh)
- Тест сессии (кнопка Test)

### 4. Groups (`/groups`)

**Возможности:**
- Просмотр всех групп
- Создание новых групп
- Информация о группах:
  - Название
  - ID группы
  - Количество участников
  - Админ

**Создание группы:**
1. Нажмите "Create Group"
2. Заполните форму:
   - Group Title
   - Admin Phone
   - Member Phones (через запятую)
3. Нажмите "Create"

### 5. Jobs (`/jobs`)

**Возможности:**
- Просмотр активных задач
- Создание новых warm-up задач

**Создание задачи:**
1. Нажмите "Create Job"
2. Заполните форму:
   - Phone Number
   - Account ID
   - Script ID (по умолчанию: warmup_script_v1)
   - Enable Group Chat (опционально)
   - Group ID (если включен Group Chat)
3. Нажмите "Create Job"

## 🎯 Основные функции

### Управление сессиями

- **Просмотр:** Все сессии отображаются в таблице
- **Фильтрация:** Можно добавить поиск (в будущем)
- **Тестирование:** Проверка работоспособности сессии

### Управление группами

- **Создание:** Через модальное окно
- **Просмотр:** Список всех созданных групп
- **Детали:** Информация о каждой группе

### Управление задачами

- **Создание:** Warm-up задачи для аккаунтов
- **Мониторинг:** Статус выполнения задач
- **Настройки:** Включение группового общения

## 🔧 API Endpoints

Все API endpoints доступны через `/api/v1/`:

- `GET /api/v1/status` - Статус системы
- `GET /api/v1/sessions` - Список сессий
- `GET /api/v1/groups` - Список групп
- `POST /api/v1/groups/create` - Создать группу
- `POST /api/v1/jobs/create` - Создать задачу
- `POST /api/v1/jobs/report` - Отчет от worker'а

## 🎨 Дизайн

Интерфейс использует:
- Современный дизайн
- Адаптивная верстка
- Темная/светлая тема (можно добавить)
- Плавные анимации

## 📱 Мобильная версия

Интерфейс адаптирован для мобильных устройств:
- Адаптивная сетка
- Мобильное меню
- Оптимизированные формы

## 🔄 Автообновление

Dashboard автоматически обновляется каждые 30 секунд для отображения актуальной информации.

## ⚙️ Настройки

Все настройки хранятся в:
- `.env` - переменные окружения
- `local-storage/` - локальные данные

## 🐛 Отладка

Если что-то не работает:

1. Проверьте логи:
   ```bash
   docker-compose logs control-api
   ```

2. Проверьте статус:
   ```bash
   curl http://localhost:8000/api/v1/status
   ```

3. Проверьте консоль браузера (F12)

## ✅ Итог

Веб-интерфейс предоставляет полный контроль над системой:
- Управление сессиями
- Создание групп
- Запуск задач
- Мониторинг статуса

Все через удобный веб-интерфейс!



