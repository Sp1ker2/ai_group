# 📱 Работа с кодами авторизации Telegram

## ⚠️ ВАЖНО: Как работает авторизация

### Проблема с кодами из обычного Telegram

**Если вы запросили код через обычное приложение Telegram:**
- ❌ Код из обычного Telegram **НЕ будет работать** с системой напрямую
- ❌ Каждый запрос кода генерирует уникальный `phone_code_hash`
- ❌ Код работает только с тем `phone_code_hash`, который был создан при запросе

### ✅ Правильные способы получения session

## Способ 1: Через веб-интерфейс (рекомендуется)

1. **Откройте веб-интерфейс**: http://localhost:8001/sessions
2. **Нажмите "Получить код"**
3. **Введите номер телефона**
4. **Система отправит код через API** (новый код придет в Telegram)
5. **Введите код из Telegram**
6. ✅ Session будет создан автоматически

**Важно:** Используйте код, который придет **после** нажатия "Получить код" в системе!

## Способ 2: Через скрипт (для кода из обычного Telegram)

Если вы уже запросили код через обычный Telegram и хотите использовать его:

```bash
# Установите переменные окружения
export TELEGRAM_API_ID="ваш_api_id"
export TELEGRAM_API_HASH="ваш_api_hash"

# Запустите скрипт
python scripts/get-session.py
```

Скрипт:
1. Запросит номер телефона
2. Отправит код через API (новый код)
3. Попросит ввести код из Telegram
4. Создаст session файл

**Или используйте прямой ввод:**

```python
# Скрипт автоматически запросит код и попросит ввести его
# Вы можете использовать код из обычного Telegram, если он свежий
```

## Способ 3: Прямой API вызов (для разработчиков)

### Если код запрошен через систему:

```bash
# 1. Получить phone_code_hash
curl -X POST http://localhost:8001/api/v1/sessions/get-code \
  -H "Content-Type: application/json" \
  -d '{"phone_number": "+79001234567"}'

# Ответ содержит phone_code_hash
# 2. Ввести код
curl -X POST http://localhost:8001/api/v1/sessions/verify-code \
  -H "Content-Type: application/json" \
  -d '{
    "phone_number": "+79001234567",
    "phone_code_hash": "полученный_hash",
    "code": "12345"
  }'
```

### Если код запрошен через обычный Telegram:

⚠️ **Этот способ может не работать**, так как нужен `phone_code_hash` от того же запроса.

**Рекомендация:** Используйте скрипт `get-session.py` или запросите код через систему.

## 🔄 Полный workflow

### Вариант A: Через веб-интерфейс

```
1. Открыть http://localhost:8001/sessions
2. Нажать "Получить код"
3. Ввести номер: +79001234567
4. Система отправит код → код придет в Telegram
5. Ввести код из Telegram
6. ✅ Session создан!
```

### Вариант B: Через скрипт

```bash
# 1. Установить переменные
export TELEGRAM_API_ID="12345"
export TELEGRAM_API_HASH="abcdef..."

# 2. Запустить скрипт
python scripts/get-session.py

# 3. Ввести номер: +79001234567
# 4. Скрипт отправит код → код придет в Telegram
# 5. Ввести код из Telegram
# 6. ✅ Session создан в local-storage/sessions/
```

## ❓ Частые вопросы

### Q: Можно ли использовать код из обычного Telegram?

**A:** Технически нет, потому что:
- Код из обычного Telegram связан с `phone_code_hash` от обычного приложения
- Система использует API и создает свой `phone_code_hash`
- Они не совместимы

**Решение:** Используйте скрипт `get-session.py`, который работает напрямую с Telegram API.

### Q: Что делать если код не подходит?

**A:** 
1. Убедитесь, что используете код, который пришел **после** запроса через систему
2. Код действителен только несколько минут
3. Если истек - запросите новый код
4. Используйте скрипт `get-session.py` для ручной авторизации

### Q: Можно ли использовать один код несколько раз?

**A:** Нет, каждый код можно использовать только один раз.

### Q: Что делать если требуется пароль 2FA?

**A:** Система автоматически запросит пароль 2FA после ввода кода. Введите пароль в появившемся поле.

## 🎯 Рекомендации

1. **Для массовой авторизации:** Используйте скрипт `batch-get-sessions.py`
2. **Для одного аккаунта:** Используйте веб-интерфейс или `get-session.py`
3. **Если код уже запрошен:** Используйте скрипт, он запросит новый код автоматически
4. **Для автоматизации:** Используйте API endpoints с правильным workflow

## 📝 Примеры

### Пример 1: Через веб-интерфейс

```
1. http://localhost:8001/sessions
2. "Получить код" → +79001234567
3. Код приходит в Telegram: 12345
4. Вводим 12345 в форму
5. ✅ Готово!
```

### Пример 2: Через скрипт

```bash
$ python scripts/get-session.py
Введите номер телефона: +79001234567
📱 Отправка кода на +79001234567...
Введите код из Telegram: 12345
✅ Session создан: 79001234567.json
```

### Пример 3: Массовая авторизация

```bash
# Создать файл с номерами
echo "+79001234567" > accounts.txt
echo "+79001234568" >> accounts.txt

# Запустить массовый скрипт
python scripts/batch-get-sessions.py accounts.txt

# Для каждого номера:
# 1. Скрипт отправит код
# 2. Вы введете код
# 3. Session будет создан
```

## ✅ Итог

**Правильный workflow:**
1. Запросить код **через систему** (веб-интерфейс или API)
2. Использовать код, который придет **после** запроса
3. Ввести код в систему
4. ✅ Session создан!

**Неправильный workflow:**
1. ❌ Запросить код через обычный Telegram
2. ❌ Попытаться использовать этот код в системе
3. ❌ Код не подойдет, так как `phone_code_hash` не совпадает

**Решение для кода из обычного Telegram:**
- Используйте скрипт `get-session.py`
- Или запросите новый код через систему



