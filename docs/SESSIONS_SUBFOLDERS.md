# 📁 Сессии в подпапках

## Структура

Сессии могут храниться в подпапках:

```
local-storage/sessions/
├── 573025288905/
│   ├── 573025288905.json
│   └── 573025288905.session
├── 573025288918/
│   ├── 573025288918.json
│   └── 573025288918.session
└── ...
```

## 🔍 Как работает поиск

### Worker (docker/android-worker/worker.py)

Worker ищет сессии в следующем порядке:

1. **Прямой путь**: `local-storage/sessions/{phone}.json`
2. **В подпапке**: `local-storage/sessions/{phone}/{phone}.json`
3. **Рекурсивный поиск**: Все `.json` файлы с именем `{phone}.json`
4. **.session файлы**: Аналогично для `.session` файлов

### Control API (docker/control-api/main.py)

API использует рекурсивный поиск:

```python
# Находит все .json файлы в подпапках
for json_file in SESSIONS_DIR.rglob("*.json"):
    # Обработка...
```

## ✅ Проверка

Все сессии автоматически обнаруживаются:
- ✅ В корне папки `sessions/`
- ✅ В подпапках `sessions/{phone}/`
- ✅ На любом уровне вложенности

## 📝 Формат JSON

Сессии в подпапках имеют стандартный формат:

```json
{
  "phone": "573025288905",
  "phone_number": "+573025288905",
  "account_id": "573025288905",
  ...
}
```

## 🚀 Использование

Просто укажите номер телефона - система найдет сессию автоматически:

```python
# Worker автоматически найдет:
# local-storage/sessions/573025288905/573025288905.json
phone_number = "+573025288905"
```

## ✅ Итог

Система полностью поддерживает сессии в подпапках и автоматически их находит!







