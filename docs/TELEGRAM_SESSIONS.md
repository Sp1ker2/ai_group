# 📱 Работа с Telegram сессиями

## ⚠️ ВАЖНО: API credentials vs Sessions

### API Credentials (api_id, api_hash) - ОДИН РАЗ!

**НЕ нужно получать для каждого аккаунта!**

- ✅ Получите **ОДИН РАЗ** на https://my.telegram.org/apps
- ✅ Используйте **ОДИНАКОВЫЕ** api_id/api_hash для всех аккаунтов
- ✅ Это credentials для вашего **приложения**, а не аккаунта

### Sessions - для каждого аккаунта

- ❌ Session нужен для **каждого аккаунта** отдельно
- ✅ Но использует **те же самые** api_id/api_hash

## 🚀 Массовое получение сессий (1000+ аккаунтов)

### Вариант 1: Автоматизированный скрипт

Создайте файл `accounts.txt` со списком номеров:

```
+79001234567
+79001234568
+79001234569
...
```

Запустите массовый скрипт:

```bash
python scripts/batch-get-sessions.py accounts.txt
```

Скрипт автоматически:
- Прочитает все номера из файла
- Для каждого получит session
- Сохранит в `session_<account_id>.json`
- Покажет прогресс

### Вариант 2: Через базу данных

Если номера в базе данных:

```python
import asyncio
from telethon import TelegramClient
from telethon.sessions import StringSession

api_id = 12345  # ОДИН для всех
api_hash = 'your_hash'  # ОДИН для всех

async def get_session_for_phone(phone):
    session = StringSession()
    client = TelegramClient(session, api_id, api_hash)
    await client.start(phone=phone)
    return client.session.save()

# Получить сессии для всех номеров из БД
phones = ['+79001234567', '+79001234568', ...]
for phone in phones:
    session_string = await get_session_for_phone(phone)
    # Сохранить в БД или файл
```

## 📋 Пошаговая инструкция

### Шаг 1: Получить Telegram API credentials (ОДИН РАЗ!)

1. Перейдите на https://my.telegram.org/apps
2. Войдите с **любым** вашим номером телефона
3. Создайте приложение
4. Скопируйте `api_id` и `api_hash`

**Эти credentials используются для ВСЕХ аккаунтов!**

### Шаг 2: Получить session для каждого аккаунта

#### Вариант A: Через скрипт (рекомендуется)

```bash
# Установить ОДИН раз
export TELEGRAM_API_ID="ваш_api_id"
export TELEGRAM_API_HASH="ваш_api_hash"

# Для одного аккаунта
python scripts/get-session.py

# Для массового получения
python scripts/batch-get-sessions.py accounts.txt
```

#### Вариант B: Вручную через Python

```python
from telethon import TelegramClient
from telethon.sessions import StringSession

# ОДИН раз для всех аккаунтов
api_id = 12345
api_hash = 'your_api_hash'

# Для каждого аккаунта
session = StringSession()
client = TelegramClient(session, api_id, api_hash)
await client.start(phone='+79001234567')
session_string = client.session.save()
```

### Шаг 3: Загрузить session в MinIO/S3

```bash
# Один файл
python scripts/upload-session-to-s3.py session_12345.json

# Или все сразу
python scripts/batch-upload-sessions.py
```

### Шаг 4: Настроить Worker

Worker автоматически:
1. Загрузит session из S3 по `account_id`
2. Использует session для авторизации
3. Выполнит warm-up скрипт
4. Отправит отчет в Control API

## 🔄 Автоматическая авторизация

После загрузки session в S3, Worker **автоматически**:

1. **Загружает session** из S3 при старте
2. **Авторизуется** используя session string
3. **Не требует** ввода кода или пароля
4. **Готов к работе** сразу после запуска

### Пример работы:

```python
# Worker автоматически делает это:
session_data = await load_session_from_s3(account_id="12345")
# session_data содержит session_string

client = TelegramClient(
    StringSession(session_data['session_string']),
    api_id=session_data['api_id'],  # ОДИН для всех
    api_hash=session_data['api_hash']  # ОДИН для всех
)

await client.start()  # Авторизация БЕЗ кода!
```

## 📁 Формат session файла

```json
{
  "account_id": "12345",
  "phone_number": "+79001234567",
  "username": "username",
  "first_name": "Имя",
  "last_name": "Фамилия",
  "session_string": "1BVtsOMwBu5...",
  "api_id": "12345",  // ОДИН для всех
  "api_hash": "abcdef1234567890"  // ОДИН для всех
}
```

## 🔐 Безопасность

⚠️ **Важно:**
- Session string = полный доступ к аккаунту
- Храните session файлы в безопасном месте
- Используйте S3 с шифрованием
- Не коммитьте session файлы в Git
- Регулярно ротируйте session (если нужно)

## 🚀 Массовая загрузка сессий

### Скрипт для загрузки всех сессий:

```python
import os
import json
from glob import glob

# Найти все session файлы
session_files = glob("session_*.json")

for session_file in session_files:
    print(f"Загрузка {session_file}...")
    upload_session_to_s3(session_file)
```

## ✅ Проверка загруженных сессий

```bash
# Через MinIO CLI
mc ls minio/telegram-sessions/

# Или через Python
from minio import Minio

client = Minio('localhost:9000', 'minioadmin', 'minioadmin', secure=False)
objects = client.list_objects('telegram-sessions')
for obj in objects:
    print(obj.object_name)
```

## 🧪 Тестирование session

```python
from telethon import TelegramClient
from telethon.sessions import StringSession
import json

# Загрузить session
with open('session_12345.json', 'r') as f:
    session_data = json.load(f)

# Создать клиент (используем ОДИНАКОВЫЕ api_id/api_hash)
client = TelegramClient(
    StringSession(session_data['session_string']),
    int(session_data['api_id']),  # ОДИН для всех
    session_data['api_hash']  # ОДИН для всех
)

async def test():
    await client.start()
    me = await client.get_me()
    print(f"Авторизован как: {me.first_name}")
    await client.disconnect()

asyncio.run(test())
```

## 📝 FAQ

### Q: Нужно ли получать api_id/api_hash для каждого аккаунта?
**A:** НЕТ! Получите ОДИН РАЗ и используйте для всех аккаунтов.

### Q: Нужно ли получать session для каждого аккаунта?
**A:** Да, каждый аккаунт имеет свой уникальный session.

### Q: Как часто нужно обновлять session?
**A:** Session действует до тех пор, пока:
- Аккаунт не сменит пароль
- Не произойдет принудительный logout
- Session не будет отозван

### Q: Могут ли аккаунты общаться между собой?
**A:** Да! После авторизации Worker может:
- Отправлять сообщения
- Читать сообщения
- Выполнять любые действия через Telegram API

### Q: Нужно ли каждый раз вводить код?
**A:** Нет! После получения session, авторизация происходит автоматически.

## 🎯 Итог

1. ✅ Получите **api_id/api_hash ОДИН РАЗ** (для всех аккаунтов)
2. ✅ Получите **session для каждого аккаунта** (можно массово)
3. ✅ Загрузите session в S3/MinIO
4. ✅ Worker автоматически использует session
5. ✅ Авторизация происходит без кода
6. ✅ Аккаунты готовы к работе!
