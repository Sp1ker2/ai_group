# Telegram Warm-up Farm - Cloud Deployment Package

Полный готовый пакет для развертывания Kubernetes-кластера для Telegram warm-up farm в облаке (РФ).

## 📦 Что включено

- ✅ **Terraform модули** для создания инфраструктуры
- ✅ **Helm charts** для всех компонентов системы
- ✅ **Kubernetes manifests** (raw, альтернатива Helm)
- ✅ **Docker образы** с готовыми Dockerfile
- ✅ **Скрипты автоматизации** развертывания
- ✅ **Полная документация** по развертыванию
- ✅ **Шаблоны запросов** в облачные провайдеры

## 🚀 Быстрый старт

### Вариант 1: Использование Makefile (рекомендуется)

```bash
# Показать все команды
make help

# Создать secrets
./scripts/create-secrets.sh

# Развернуть все компоненты
make deploy

# Проверить статус
make status
```

### Вариант 2: Пошаговое развертывание

```bash
# 1. Создать secrets
./scripts/create-secrets.sh

# 2. Развернуть базовые компоненты
kubectl apply -f k8s/namespace.yaml
kubectl apply -f k8s/redis/
kubectl apply -f k8s/postgres/

# 3. Развернуть через Helm
helm install control-node ./helm/control-node -n telegram-farm
helm install wg-proxy ./helm/wg-proxy -n telegram-farm

# 4. Проверить
make test
```

См. [docs/QUICKSTART.md](docs/QUICKSTART.md) для детальной инструкции.

## 📋 Компоненты системы

- **Control Node**: API (FastAPI) + UI (React) для управления
- **wg-proxy**: DaemonSet для WireGuard туннелирования через российские IP
- **android-worker**: Job pods для выполнения Telegram warm-up задач
- **Redis**: Очередь задач (StatefulSet, 3 nodes)
- **Postgres**: База данных (StatefulSet, HA)
- **Monitoring**: Prometheus + Grafana (опционально)

## 🏗️ Архитектура

```
Operator (Web UI) → Control Node → Queue (Redis) → Kubernetes Cluster
                                                          ↓
                                              DaemonSet wg-proxy
                                                          ↓
                                              Job Pods (android-worker)
                                                          ↓
                                              Telegram servers (через RF IP)
```

Подробнее: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)

## 📚 Документация

- **[QUICKSTART.md](docs/QUICKSTART.md)** - Быстрый старт за 5 минут
- **[DEPLOYMENT.md](docs/DEPLOYMENT.md)** - Полное руководство по развертыванию
- **[ARCHITECTURE.md](docs/ARCHITECTURE.md)** - Архитектура системы
- **[CLOUD_REQUEST_TEMPLATE.md](docs/CLOUD_REQUEST_TEMPLATE.md)** - Шаблоны запросов в облако
- **[PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md)** - Структура проекта

## 🔧 Требования

- **Kubernetes** >= 1.24
- **kubectl** настроен и подключен к кластеру
- **Helm** >= 3.8 (опционально, но рекомендуется)
- **Terraform** >= 1.0 (для создания инфраструктуры)
- **Docker** (для сборки образов)

## 📁 Структура проекта

```
.
├── terraform/          # Terraform модули
├── helm/              # Helm charts
├── k8s/               # Raw Kubernetes manifests
├── docker/            # Docker образы
├── scripts/           # Скрипты развертывания
└── docs/              # Документация
```

Подробнее: [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md)

## 🎯 Основные команды

```bash
make help              # Показать все команды
make deploy            # Развернуть все компоненты
make build-worker      # Собрать Docker образ android-worker
make test              # Запустить smoke тесты
make status            # Показать статус развертывания
make check-ip          # Проверить исходящий IP
```

## 🔐 Безопасность

⚠️ **Важно**: Перед развертыванием:

1. Создайте все необходимые secrets (см. `k8s/secrets/README.md`)
2. Обновите конфигурацию WireGuard в `k8s/wg-proxy/configmap.yaml`
3. Настройте security groups и network policies
4. Используйте Sealed Secrets или External Secrets Operator для production

## 📞 Запрос ресурсов в облако

Готовые шаблоны для отправки в облачные провайдеры:
- Краткий запрос
- Детальное письмо
- Чек-лист для провайдера
- Acceptance criteria

См. [docs/CLOUD_REQUEST_TEMPLATE.md](docs/CLOUD_REQUEST_TEMPLATE.md)

## 🧪 Тестирование

```bash
# Smoke тесты
make test
# или
./scripts/smoke-test.sh

# Проверка исходящего IP
make check-ip
```

## 📝 Лицензия и использование

Этот пакет предназначен для легального использования с явным opt-in пользователей. Любое массовое рассылание без согласия может привести к юридическим последствиям.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи: `kubectl logs -f <pod-name> -n telegram-farm`
2. Проверьте события: `kubectl get events -n telegram-farm`
3. См. раздел Troubleshooting в [DEPLOYMENT.md](docs/DEPLOYMENT.md)

