# 🚀 Быстрый старт: Веб-интерфейс

## 📱 Как открыть UI

### Вариант 1: Через Docker Compose (рекомендуется)

```bash
# Запустить все сервисы
docker-compose up -d

# Или только Control API
docker-compose up -d control-api
```

**Откройте в браузере:** http://localhost:8000

### Вариант 2: Локально (без Docker)

```bash
# Установить зависимости
cd docker/control-api
pip install fastapi uvicorn jinja2 python-multipart aiofiles

# Запустить
python main.py
```

**Откройте в браузере:** http://localhost:8000

## 🌐 Доступные страницы

### Главная страница
- **URL:** http://localhost:8000
- **Описание:** Обзор системы, быстрый доступ

### Dashboard
- **URL:** http://localhost:8000/dashboard
- **Описание:** Статистика, последние сессии и группы

### Sessions (Сессии)
- **URL:** http://localhost:8000/sessions
- **Описание:** Просмотр всех сессий, тестирование

### Groups (Группы)
- **URL:** http://localhost:8000/groups
- **Описание:** Управление группами, создание новых

### Jobs (Задачи)
- **URL:** http://localhost:8000/jobs
- **Описание:** Создание и управление warm-up задачами

## 🔧 API Endpoints

### Статус системы
```
GET http://localhost:8000/api/v1/status
```

### Список сессий
```
GET http://localhost:8000/api/v1/sessions
```

### Список групп
```
GET http://localhost:8000/api/v1/groups
```

## ⚠️ Если не работает

### Проблема: Docker не запущен

**Решение 1:** Запустить Docker Desktop

**Решение 2:** Запустить локально:
```bash
cd docker/control-api
pip install -r requirements.txt  # или установить вручную
python main.py
```

### Проблема: Порт 8000 занят

**Решение:** Изменить порт в `docker-compose.yml` или в `main.py`:
```python
uvicorn.run(app, host="0.0.0.0", port=8001)  # Другой порт
```

### Проблема: Не видно сессий

**Решение:** Проверить что сессии в `local-storage/sessions/` и что они в правильном формате

## ✅ Проверка работы

1. Откройте http://localhost:8000
2. Должна загрузиться главная страница
3. Перейдите в Dashboard - должны быть видны сессии
4. Перейдите в Sessions - должны отображаться все сессии

## 🎯 Итог

Просто откройте **http://localhost:8000** в браузере!





